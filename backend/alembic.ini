# Alembic configuration for the artefact store.
# The runtime URL is injected by app.db.init_db() from DATABASE_URL / FX_DB_URL;
# the value below is only the standalone-CLI default.
[alembic]
script_location = alembic
sqlalchemy.url = sqlite:///./file_explorer.db
